# Local shared packages (editable installs, resolved via relative path)
-e ../../packages/shared-schema
-e ../../packages/shared-utils

pytest>=8.0,<9.0

httpx>=0.27,<0.28
boto3>=1.34,<2.0

# test-only: mock HTTP (respx) and mock S3/SQS (moto) so tests never
# touch the real network or real AWS
respx>=0.20,<0.22
moto[s3,sqs]>=5.0,<6.0
