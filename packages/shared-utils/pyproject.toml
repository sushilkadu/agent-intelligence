[project]
name = "shared-utils"
version = "0.1.0"
description = "Shared utility helpers for Agent Intelligence Python services"
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["shared_utils*"]
