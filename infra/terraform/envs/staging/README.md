# staging environment

Placeholder. This environment mirrors the structure of `../dev` (backend
config wired to a staging state bucket/lock table, plus a call into the
`network` module) once we get there. Not implemented in Phase 0 -- only
`envs/dev` actually calls the `network` module for now.
